#include <stdio.h>
/**
 * main - entry point
 * Return: always 0 (Success)
 */

int main(void)
{
	int num;

	while (num < 10)
	{
		printf("%d", num);
		num++;
	}
	        putchar('\n');
	return (0);
}

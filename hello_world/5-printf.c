#include <stdio.h>

/**
 * main - prints a string sing the printf command
 *
 * Return: 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a peice of art,\n");
	fprintf( stderr, "" )
	return (0);
}
